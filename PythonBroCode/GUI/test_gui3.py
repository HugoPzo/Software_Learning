from tkinter import *
from turtledemo.nim import randomrow

window = Tk()
window["bg"] = "black"

num_buttons = [1, 2, 3, 4, 5, 6, 7, 8, 9, 0]
for num in num_buttons:
    if num_buttons[]
    button_num = Button(window, width=10, text=num)
    button_num.pack(side=LEFT)

window.mainloop()